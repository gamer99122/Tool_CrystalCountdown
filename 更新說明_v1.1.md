# 桌面公告小工具 - 更新說明 v1.1

## 🎉 新功能

### ✨ 視窗可拖拉移動並記憶位置

現在你可以自由拖動視窗到任何位置，程式會自動記住你的選擇！

---

## 📋 功能說明

### 1️⃣ 拖拉視窗
- **操作方式**：點擊視窗任意位置（除了關閉按鈕），按住滑鼠左鍵拖動
- **適用區域**：整個視窗都可以拖拉
- **即時儲存**：放開滑鼠後立即儲存新位置

### 2️⃣ 位置記憶
- **自動儲存**：每次移動視窗後自動儲存位置到 `position.txt`
- **自動載入**：下次啟動時會在上次儲存的位置顯示
- **預設位置**：首次執行時預設在右下角

### 3️⃣ 智慧定位
- **螢幕邊界檢查**：確保視窗不會跑到螢幕外
- **工作列考慮**：自動避開工作列區域
- **多螢幕支援**：在主螢幕範圍內有效

---

## 📂 檔案說明

### `position.txt` - 位置儲存檔
```
1420
850
```
- 第一行：視窗左邊距離螢幕左側的像素
- 第二行：視窗上邊距離螢幕頂部的像素

**注意**：
- 此檔案會自動生成，無需手動建立
- 可以手動刪除此檔案，程式會恢復到預設右下角位置
- 與 `config.txt` 互相獨立，互不影響

---

## 🎯 使用場景

### 場景 1：避開桌面圖示
```
問題：右下角有常用的桌面檔案
解決：拖動視窗到左下角或其他位置
```

### 場景 2：多螢幕工作
```
問題：雙螢幕環境，希望顯示在副螢幕
解決：拖動視窗到副螢幕的任意位置（目前僅支援主螢幕）
```

### 場景 3：個人化配置
```
問題：每個人習慣不同的位置
解決：每個人可以自訂自己喜歡的位置
```

---

## 🔧 技術細節

### 位置儲存格式
```
檔案：position.txt
格式：純文字，每行一個數值
範例：
1420  ← Left (X 座標)
850   ← Top (Y 座標)
```

### 位置驗證邏輯
```csharp
// 確保視窗至少有一部分在螢幕內
bool horizontalValid = left + width > workArea.Left && left < workArea.Right;
bool verticalValid = top + height > workArea.Top && top < workArea.Bottom;
```

### 載入順序
```
1. 程式啟動
2. 嘗試讀取 position.txt
3. 如果檔案存在且位置有效 → 使用儲存的位置
4. 如果檔案不存在或位置無效 → 使用右下角預設位置
```

---

## 🆚 版本比較

| 功能 | v1.0 | v1.1 |
|------|------|------|
| 視窗可拖拉 | ❌ | ✅ |
| 位置記憶 | ❌ | ✅ |
| 預設位置 | 中央 | 右下角 |
| 關閉按鈕 | ✅ | ✅ |
| 自動顯示/隱藏 | ✅ | ✅ |

---

## 💡 常見問題

### Q: 如何重設到預設位置？
A: 刪除 `position.txt` 檔案，重新啟動程式即可恢復到右下角預設位置。

### Q: 視窗跑到螢幕外了怎麼辦？
A: 程式有邊界檢查機制，如果儲存的位置無效會自動使用右下角。如果還是有問題，刪除 `position.txt` 即可。

### Q: 可以手動編輯 position.txt 嗎？
A: 可以，但必須確保數值有效。建議使用拖拉功能自動儲存。

### Q: 為什麼拖動後位置沒有儲存？
A: 檢查 `position.txt` 是否有寫入權限。如果在 Program Files 目錄，可能需要管理員權限。

### Q: 支援多螢幕嗎？
A: 目前僅支援主螢幕範圍內的位置驗證。

---

## 🚀 未來改進方向

- [ ] 支援多螢幕位置記憶
- [ ] 使用 JSON 格式儲存更多設定
- [ ] 記住視窗大小
- [ ] 快速定位快捷鍵（例如：按 Ctrl 回到預設位置）
- [ ] 位置預設選項（左上、右上、左下、右下）

---

## 📝 更新日誌

### v1.1 (2026/01/09)
- ✨ 新增視窗拖拉功能
- ✨ 新增位置自動儲存
- ✨ 新增位置自動載入
- ✨ 新增螢幕邊界檢查
- 🐛 修正視窗初始位置為右下角

### v1.0 (2026/01/09)
- 🎉 首次發布
- ✨ 水晶玻璃視覺效果
- ✨ 桌面智慧顯示/隱藏
- ✨ 日期倒數計時
- ✨ 關閉按鈕

---

## 📞 聯絡與回饋

如有問題或建議，歡迎回饋！
